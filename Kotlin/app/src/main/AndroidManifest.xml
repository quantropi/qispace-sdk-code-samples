<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name = ".sequrActivity"
            android:label = "Sequr Page"
            android:theme = "@style/Theme.AppCompat"/>

        <activity android:name = ".qeepActivity"
            android:label= "Qeep Page"
            android:theme = "@style/Theme.AppCompat"/>

        <activity android:name=".masqKemActivity"
            android:label="Masq-KEM Page"
            android:theme="@style/Theme.AppCompat"/>

        <activity android:name=".masqDsActivity"
            android:label="Masq-DS Page"
            android:theme="@style/Theme.AppCompat"/>


    </application>

</manifest>